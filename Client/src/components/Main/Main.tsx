import React from "react";

import './Main.scss';
import Calendar from "../Calendar/Calendar";

const Main: React.FC = () => {
    return(
        <main>
            <Calendar/>
        </main>
    )
};

export default Main;